#!/usr/bin/env node

// UserPromptSubmit hook: reminds LLM to stay in caveman ultra mode.

process.stdout.write(JSON.stringify({
    hookSpecificOutput: {
        hookEventName: "UserPromptSubmit",
        additionalContext: "CAVEMAN MODE ACTIVE (ultra). " +
"Drop articles/filler/pleasantries/hedging. Fragments OK. " +
"Abbreviate (DB/auth/config/req/res/fn/impl), strip conjunctions, " +
"arrows for causality (X → Y), one word when one word enough. " +
"Code/commits/security: write normal."
    }
}));
